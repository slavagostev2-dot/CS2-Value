{"from":"0.22.14","version":"0.22.15","files":[],"delete":[],"patches":[{"path":"VERSION.txt","before_sha256":"eb17eb3d3fdbf40cc001f8415f21476be284c96047240f3d9917eeaa2a636472","after_sha256":"9544ced7666710623f8a4789090117565559848245609a88b3ba795a8d87233d","replacements":[{"old":"0.22.14","new":"0.22.15","count":1}]},{"path":"pyproject.toml","before_sha256":"c3e3af45d2d841fb8238d068f90a0a2cf2f6a19bda6f70ab1c4f2c7826d50a76","after_sha256":"eb9cb1f6e288e9f0ab5824df2eb9328211f3cf70dde36b5861239d3b6059be47","replacements":[{"old":"version = \"0.22.14\"","new":"version = \"0.22.15\"","count":1}]},{"path":"src/cs2_value/__init__.py","before_sha256":"8f275afdca9265bc1ec56b770131a5ec3b850393f21fe903417ef2baee15e119","after_sha256":"8342c967d63f11c96333739f44d584bc9972fbbf1ce98dc994da43c3dcc423c8","replacements":[{"old":"__version__ = \"0.22.14\"","new":"__version__ = \"0.22.15\"","count":1}]},{"path":"src/cs2_value/offstage_dom_veto.py","before_sha256":"ec25319446a55e483ed206a049f676fc17fb920b92bce0c6fffc2c6797f8d9e9","after_sha256":"2b3e23c63f2c69d01ca651ddd0c861071e97b2e5e6e75f0b9a60711eb235211f","replacements":[{"old":"    assets_b = set(team_assets.get(\"b\") or ())\n    if not assets_a or not assets_b:\n        return ()\n\n","new":"    assets_b = set(team_assets.get(\"b\") or ())\n    if not assets_a or not assets_b:\n        return ()\n\n    # Preferred live path: infer picker ownership only from the small rendered\n    # Pick badges.  Large logos elsewhere in a map card can represent the map\n    # winner and must never be used as picker evidence.  When badge evidence is\n    # present we do not fall back to broad card logos.\n    pick_badges = payload.get(\"pick_badges\") or ()\n    if pick_badges:\n        series_text = sorted(\n            (\n                action for action in text_veto\n                if action.action in {\"pick\", \"decider\"}\n                and action.series_map_order is not None\n            ),\n            key=lambda action: int(action.series_map_order or 0),\n        )\n        if (\n            best_of == 3\n            and len(series_text) == 3\n            and [action.series_map_order for action in series_text] == [1, 2, 3]\n            and sum(action.action == \"pick\" for action in series_text) == 2\n            and sum(action.action == \"decider\" for action in series_text) == 1\n        ):\n            pick_rows = [action for action in series_text if action.action == \"pick\"]\n            badge_by_map: dict[str, dict] = {}\n            for item in pick_badges:\n                raw_name = str(item.get(\"map\") or \"\").strip()\n                canonical = next(\n                    (name for name in KNOWN_MAPS if name.casefold() == raw_name.casefold()),\n                    None,\n                )\n                if canonical is None:\n                    continue\n                key = canonical.casefold()\n                score = int(item.get(\"score\") or 0)\n                current = badge_by_map.get(key)\n                if current is None or score > int(current.get(\"score\") or 0):\n                    badge_by_map[key] = dict(item)\n\n            owners: list[str | None] = []\n            both_badges_present = True\n            for action in pick_rows:\n                item = badge_by_map.get(action.map_name.casefold())\n                if item is None:\n                    both_badges_present = False\n                    owners.append(None)\n                    continue\n                badge_assets = set(item.get(\"assets\") or ())\n                has_a = bool(badge_assets & assets_a)\n                has_b = bool(badge_assets & assets_b)\n                if has_a and not has_b:\n                    owners.append(team_a)\n                elif has_b and not has_a:\n                    owners.append(team_b)\n                else:\n                    owners.append(None)\n\n            # Standard BO3 complement: both Pick badges must exist and exactly\n            # one must identify a team directly.  The other pick is the opposite\n            # team.  This is never used for BO5.\n            if both_badges_present:\n                if owners.count(team_a) == 1 and owners.count(team_b) == 0 and owners.count(None) == 1:\n                    owners[owners.index(None)] = team_b\n                elif owners.count(team_b) == 1 and owners.count(team_a) == 0 and owners.count(None) == 1:\n                    owners[owners.index(None)] = team_a\n\n            if owners.count(team_a) == 1 and owners.count(team_b) == 1:\n                owner_for = {\n                    pick_rows[0].map_name.casefold(): owners[0],\n                    pick_rows[1].map_name.casefold(): owners[1],\n                }\n                return tuple(\n                    VetoAction(\n                        map_name=action.map_name,\n                        action=action.action,\n                        team_name=(\n                            None if action.action == \"decider\"\n                            else owner_for.get(action.map_name.casefold())\n                        ),\n                        action_order=index + 1,\n                        series_map_order=index + 1,\n                    )\n                    for index, action in enumerate(series_text)\n                )\n        return ()\n\n","count":1},{"old":"  const maps = [];\n  const all = Array.from(document.querySelectorAll('body *'));\n  for (const map of knownMaps) {\n    const target = norm(map);\n","new":"  const maps = [];\n  const all = Array.from(document.querySelectorAll('body *'));\n\n  // Picker ownership comes only from the small Pick badge.  Do not inspect the\n  // surrounding map-card logo because after a map finishes that can be the winner.\n  const mapForElement = el => {\n    let cur = el;\n    for (let depth = 0; cur && depth < 9; depth++, cur = cur.parentElement) {\n      const txt = norm(cur.innerText || '');\n      const present = knownMaps.filter(map => txt.includes(norm(map)));\n      if (present.length === 1) return present[0];\n    }\n    return null;\n  };\n  const bestPickBadgeRoot = el => {\n    let cur = el;\n    let best = null;\n    for (let depth = 0; cur && depth < 4; depth++, cur = cur.parentElement) {\n      const r = cur.getBoundingClientRect();\n      const txt = norm(cur.innerText || '');\n      const a = assets(cur);\n      let score = 0;\n      if (txt === 'pick') score += 40;\n      else if (txt.includes('pick') && txt.length <= 18) score += 20;\n      if (a.length) score += 100;\n      if (r.width > 0 && r.width <= 110 && r.height > 0 && r.height <= 50) score += 35;\n      if (!best || score > best.score) best = {root: cur, score};\n    }\n    return best;\n  };\n  const pickBadges = [];\n  const pickTextEls = all.filter(el => {\n    if (!visible(el)) return false;\n    const d = norm(directText(el));\n    const i = norm(el.innerText || '');\n    return d === 'pick' || i === 'pick';\n  });\n  for (const el of pickTextEls) {\n    const best = bestPickBadgeRoot(el);\n    if (!best) continue;\n    const map = mapForElement(best.root);\n    if (!map) continue;\n    pickBadges.push({\n      map,\n      score: best.score,\n      order: all.indexOf(el),\n      assets: assets(best.root),\n    });\n  }\n\n  for (const map of knownMaps) {\n    const target = norm(map);\n","count":1},{"old":"  return {\n    team_assets: {a: teamAssetSet(teamA), b: teamAssetSet(teamB)},\n    maps,\n  };\n","new":"  return {\n    team_assets: {a: teamAssetSet(teamA), b: teamAssetSet(teamB)},\n    pick_badges: pickBadges,\n    maps,\n  };\n","count":1},{"old":"        debug(f\"DOM debug: {team_a} assets={len(assets_a)}, {team_b} assets={len(assets_b)}\")\n        best = {}\n        for item in payload.get(\"maps\") or ():\n            name = str(item.get(\"map\") or \"\").strip()\n            score = int(item.get(\"score\") or 0)\n","new":"        debug(f\"DOM debug: {team_a} assets={len(assets_a)}, {team_b} assets={len(assets_b)}\")\n        best = {}\n        debug_items = payload.get(\"pick_badges\") or payload.get(\"maps\") or ()\n        for item in debug_items:\n            name = str(item.get(\"map\") or \"\").strip()\n            score = int(item.get(\"score\") or 0)\n","count":1},{"old":"                samples.append(text)\n            debug(\n                f\"  DOM map {item.get('map')}: assets={len(assets)}, \"\n                f\"A-intersection={len(assets & assets_a)}, B-intersection={len(assets & assets_b)}, \"\n                f\"sample={', '.join(samples) if samples else '-'}\"\n","new":"                samples.append(text)\n            debug(\n                f\"  DOM Pick {item.get('map')}: assets={len(assets)}, \"\n                f\"A-intersection={len(assets & assets_a)}, B-intersection={len(assets & assets_b)}, \"\n                f\"sample={', '.join(samples) if samples else '-'}\"\n","count":1}]}]}